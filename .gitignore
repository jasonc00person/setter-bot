# API keys and local config
.mcp.json
.env

# Fathom MCP (cloned dependency, has its own .git)
tools/fathom-mcp/

# Call transcripts (contain sensitive sales call data)
call-transcripts/
tools/whisper-mcp/node_modules/
